import React from "react";

export default function Users() {
  return (
    <div className="page">
      <div className="pageTitle">Usuarios</div>
      <div className="pageSub">Buscador + acciones (después con Edge Functions).</div>

      <div className="panel">
        <div className="empty">Listo para construir la sección Usuarios.</div>
      </div>
    </div>
  );
}
