import React from "react";

export default function Users() {
  return (
    <div className="page">
      <div className="pageTitle">Usuarios</div>
      <div className="pageSub">Buscador + cambio de email/pass (después con Edge Functions).</div>

      <div className="panel">
        <div className="empty">Listo para armar buscador y ficha de usuario.</div>
      </div>
    </div>
  );
}
